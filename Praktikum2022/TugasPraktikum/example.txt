from function import product as add_p
import sqlite3
con = sqlite3.connect('data/test.db')
cur = con.cursor()

#add_p.add(input('Masukkan Product ID : '), input('Masukkan Nama Product : '), input('Masukkan Price : '))

#product = 'SELECT * from product'
#cur.execute(product)
#pro_data = cur.fetchall()
#for row in pro_data:
#   print(row)

cur.execute('DELETE FROM product WHERE id=12')
con.commit()
con.close()
